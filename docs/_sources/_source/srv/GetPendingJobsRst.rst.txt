GetPendingJobs srv
==================

The `GetPendingJobs.srv`_ is a service message which takes in no arguments and returns an integer 'Job count' as well as a list of 'PendingJobs' messages.

The service is hosted by the Job Manager.

.. _GetPendingJobs.srv: ../../srv/GetPendingJobs.html