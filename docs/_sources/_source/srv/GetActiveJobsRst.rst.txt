GetActiveJobs srv
=================

The `GetActiveJobs.srv`_ is a service message which takes in no arguments and returns an integer 'Job count' as well as a list of all Active Jobs in the using the 'ActiveJobs' message type.

The service is hosted by the Job Manager.

.. _GetActiveJobs.srv: ../../srv/GetActiveJobs.html